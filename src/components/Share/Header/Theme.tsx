import { useTheme } from 'next-themes'
import React from 'react'

function Theme() {
    const theme = useTheme();
  return (
    <div>
      {/* dropdown menu */}
    </div>
  )
}

export default Theme
